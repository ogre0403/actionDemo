name: Close PR on files which are not allowed to edit

on:
  pull_request_target:
    types: [opened, reopened]
    paths:
      - ".validate/*"
      - ".github/workflows/*"

jobs:
  close-pr:
    runs-on: "ubuntu-latest"
    steps:
      - uses: superbrothers/close-pull-request@v3
        with:
          comment: "hi. please do not touch '.validate/*' and '.github/workflows/*'."
