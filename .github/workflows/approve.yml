name: Automatic Approve
on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  automatic-approve:
    name: Automatic Approve
    runs-on: ubuntu-latest
    steps:
      - name: Automatic Approve
        uses: mheap/automatic-approve-action@v1
        with:
          token: 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
          workflows: "kind5.yml"
